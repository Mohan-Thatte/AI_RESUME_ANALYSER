# Get a key from https://console.groq.com/keys
# Required: without this, POST /api/match will return a 502 error.
# There is no mock/fallback mode - every request is analyzed live by Groq.
GROQ_API_KEY=your_groq_api_key_here
